# REPL API

**Document ID:** `SPEC-003` **Status:** Implemented **Last Updated:** 2026-08-30 **Code:** `src/millstrand/repl.clj`, `src/millstrand/api/*.alpha`, `src/millstrand/test`

## SPEC-003.P1 Purpose

The REPL API gives coding agents and human developers an interactive Clojure interface over the selected weaver workspace: live registration verbs, explicit burn recovery, and session machinery for connecting a JVM to one weaver.

## SPEC-003.P2 Interface

`millstrand.repl` publishes three groups of helpers.

Registration verbs — the `millstrand.api.*.alpha` matrix with the runtime implied, in-process only:

```clojure
register-op!       replace-op!       unregister-op!
register-query!    replace-query!    unregister-query!
register-pattern!  replace-pattern!  unregister-pattern!
register-hook!     replace-hook!     unregister-hook!
register-handler!  replace-handler!  unregister-handler!
```

Burn recovery:

```clojure
burn-by-ids!
burn-history
recent-burns
```

Session machinery:

```clojure
connect!
connected-config-dir
connected-opts
eval-source-forms!
-main
```

Strand reads and mutations are not part of this surface. Interactive callers use the `strand` CLI,
and in-process code uses the explicit-runtime `millstrand.api.*.alpha` verbs.

## SPEC-003.P3 Contracts

- **SPEC-003.C1:** `connect!` selects one active weaver connection by Millstrand workspace for explicit standalone Clojure client/test workflows. It requires an explicit selected workspace and optional state metadata supplied directly by those helpers. It never accepts a database path and never falls back to an XDG global workspace. `connect!` only records the selection: callers use `millstrand.core.client` with `connected-config-dir` and `connected-opts` to operate on it. Default `mill weaver repl` does not call `connect!`.
- **SPEC-003.C2:** `mill weaver repl` requires a running `mill`, asks it to resolve the selected workspace, verify that workspace's weaver is running, and return nREPL metadata, then attaches the user's terminal to the selected weaver nREPL endpoint. Mill does not proxy nREPL. Any launched attach client is transport/UI only: user forms are evaluated in the weaver JVM, in the neutral `user` namespace with `millstrand.repl` aliased as `repl`, not in a separate local runtime or through the fixed API bridge.
- **SPEC-003.C3:** `mill weaver repl --stdin` attaches to the selected running weaver nREPL, reads and evaluates top-level stdin forms in order in that weaver's neutral `user` namespace, prints one direct normal Clojure result per form, and exits non-zero on read, eval, or transport failure. Callers that want one machine-readable payload should wrap work in one top-level `do` or `let`.
- **SPEC-003.C4:** `millstrand.repl`'s registration verbs imply the active in-process runtime and are unavailable to a standalone connected client. Without that runtime they fail loudly, naming the verb, whether the session is standalone or connected, and the `millstrand.core.client` alternative. The `millstrand.api.*.alpha` domain namespaces do not perform implicit runtime discovery or connected-client routing; trusted in-process callers pass a runtime explicitly. `connect!` plus `millstrand.core.client` is the standalone Clojure client/test path. Weaver and transport failures surface loudly as Clojure exceptions.

Earlier drafts exposed strand CRUD, query execution, pattern reads, weaving, and relation declarations from `millstrand.repl`. Those helpers are withdrawn. The interface above is exhaustive; `burn-by-ids!` is the one retained recovery write.

- **SPEC-003.C9:** `register-query!`, `replace-query!`, and `unregister-query!` are `millstrand.repl`'s runtime-implied, in-process counterparts to the explicit-runtime `millstrand.api.graph.alpha` verbs. They claim, deliberately replace, or retract the current direct owner's query entry; the complete owner-partition contract is SPEC-003.C23.
- **SPEC-003.C12:** Direct query registrations last only for the active weaver lifetime. After a weaver restart, reload trusted config for durable authoring-form declarations or call the explicit-runtime or in-process registration verbs again.
- **SPEC-003.C13a:** Query predicates include direct edge-existence forms `[:edge/out relation target-query]` and `[:edge/in relation source-query]`. `relation` is a valid relation-name string or a `[:param :name]` reference resolving to one. Endpoint queries are strand-local and fail loudly if they contain nested edge predicates.
- **SPEC-003.C17:** `millstrand.api.runtime.alpha` exposes `module!`, the owner-complete collection helpers `collect-entry!`, `collect-lifecycle!`, and `collect-kind!`, the activation functions `refresh!`, `plan`, and `status`, the advanced code-only `reload-code!` seam, runtime-local `spool-state`, generation-classloader `resolve-var`, and runtime time through `clock` and `now`. It exposes no dependency editor, package operation, approval projection, acquisition result, or live classpath mutation. The collection helpers are passive outside module source collection. C25–C26c below own the activation and result contracts.
- **SPEC-003.C17a:** `millstrand.api.clock.alpha` exposes the Clock capability — a validated value, not a protocol, so re-evaluating the namespace never strands a live clock the runtime already holds. `(now clock)` returns a `java.time.Instant`, and `(sleep! clock duration)` sleeps or advances for a non-negative `java.time.Duration` and returns nil. Zero Duration is valid. `(clock now-fn sleep-fn)` builds one, `(clock? value)` tests one, and `(system-clock)` returns the real production implementation. Invalid durations fail loudly.
- **SPEC-003.C17b:** `(millstrand.api.spool.alpha/poll-until! clock opts)` accepts exactly `:timeout-ms`, `:poll-ms`, `:check`, `:pred->result`, and `:on-timeout`. Timeout is a non-negative integer and cadence is a positive integer. It derives an Instant deadline from the supplied Clock, checks immediately, returns the first non-nil projected result, calls `on-timeout` with the last value at or after the deadline, and otherwise sleeps on the Clock before checking again. It validates the Clock and complete option boundary before polling.
- **SPEC-003.C17d:** A public var named `spool` in a module-loadable namespace is rejected in source and image modes with the module key, namespace, and replacement-form guidance. Module declarations carry no callback entry points.
- **SPEC-003.C17e:** `millstrand.api.millstrand.alpha` owns six core authoring families: `defop`/`use-op!`/`defop!`, `defquery`/`use-query!`/`defquery!`, `defpattern`/`use-pattern!`/`defpattern!`, `defhook`/`use-hook!`/`defhook!`, `defhandler`/`use-handler!`/`defhandler!`, and `defbin`/`use-bin!`/`defbin!`. An inert definition defines exactly the supplied Var name and contributes nothing. Function-backed declarations bind the function at that name; no form synthesizes a second handler name. A bang form defines the same Var and selects it into the current module source; both definition forms return the installed Var. A typed use form accepts one or more qualified or local symbols resolving to Vars, validates protocol, family, kind, key, and entry, contributes them in argument order, and returns the selected Vars as a vector. Registry use forms accept only a leading map closed to boolean `:override?`; lifecycle use forms are covered by C17f. Repeated top-level selections retain the deterministic later-replaces-earlier rule, and the later selection's override intent is retained.
- **SPEC-003.C17f:** `millstrand.api.lifecycle.alpha` owns `defseed`/`use-seed!`/`defseed!`, `defresource`/`use-resource!`/`defresource!`, and `defreconcile`/`use-reconcile!`/`defreconcile!`, plus the closed specs for declarations, kinds, phases, statuses, and data-first projections. Inert and bang forms use the existing name, docstring, and declaration-option grammar and return the installed Var. Lifecycle selection has no override option and typed use forms accept declaration Var references only. A seed names one idempotent `:apply` symbol. A resource names paired `:open` and `:close` symbols, optional `:after` effect dependencies, and `:scope :module|:runtime` with module scope as the default. A reconcile effect names `:read-desired`, `:read-actual`, `:apply`, and `:on-removed` symbols plus optional `:trigger-kinds` and `:after`. Every callable is a fully qualified symbol. Forms define ordinary public Vars and collect declarations only while the selected module source is evaluated; image activation replays the retained declaration record. Outside that collection the forms are passive: they validate and return their declaration without recording anything, matching contribution authoring forms during code reload. Declaration shape is checked wherever a form is evaluated; unknown options and malformed declarations fail on the spot, while duplicate effect ids and evaluation from a namespace foreign to the selected module source fail before publication.
- **SPEC-003.C17g:** `millstrand.api.authoring.alpha/defauthoring` defines the three public macros for an open registry family from one noun and a mode-aware builder: `(defauthoring noun builder-bindings & plan-body)`. The builder binding vector has one reserved first symbol, which receives `:define` or `:define-and-use`; the generated definition macros expose the remaining bindings as their user arglist. The builder returns a closed expansion plan with `:name`, `:definition`, `:kind`, `:key`, `:entry`, and `:use-options` forms. `:name` is the exact simple symbol defined by `:definition`; `:use-options` is closed to `:override?`. Invalid plans fail during macro expansion, and invalid declarations or selection options fail before definition or collection. Workflow and Chime route `:override?` from their existing options position; Cron uses `(defjob name doc job)`, `(defjob! name doc use-options job)`, and the standard leading use-options map. Generated domain forms share the descriptor, typed-selection, and return contracts of the core forms.
- **SPEC-003.C17h:** Every authoring Var carries one printable descriptor under `:millstrand.api.authoring.alpha/declaration`. The closed descriptor has protocol `1`, a qualified `:family`, `:channel` `:registry` or `:lifecycle`, public `:kind` and `:key`, normalized printable `:entry`, and fully qualified `:var`; the Var root remains the natural function or declaration value. Definition and selection consult the public specs `::declaration`, `::registry-use-options`, `::builder-bindings`, `::expansion-plan`, `::selection`, and `::selected-vars`, as well as each kind's entry spec. Relational checks such as exact names, plan consistency, and duplicate effective keys remain explicit. Rejections throw `ExceptionInfo` with a boundary-specific message and structured `:reason` plus the offending form, symbol, or value; source-ownership failures include module, namespace, and file context.
- **SPEC-003.C18:** Every `millstrand.api.runtime.alpha` helper that acts on a runtime takes that runtime first. Trusted `init.clj`, `init.local.clj`, module code, and in-process tools capture the active runtime once with `millstrand.api.current.alpha/runtime` and thread it explicitly. Connected clients do not route these calls through `millstrand.api.runtime.alpha`. The existing `:millstrand.api.current.alpha/runtime` and `:millstrand.api.current.alpha/runtime-or-nil` specs and function docstrings continue to own and document that capture boundary.
- **SPEC-003.C19:** SPEC-005 owns namespace membership and compatibility: SPEC-005.C2 lists the blessed `millstrand.api.*.alpha`, `millstrand.test.alpha`, and `millstrand.repl` tiers; SPEC-005.C3–C4 separate shipped spools from userland spools; SPEC-005.C5 and C5b place `millstrand.core.*` and `millstrand.api.*.internal` outside the public contract. Accretion within each alpha subnamespace is SPEC-005.P1/C2, and a removal or reshape follows SPEC-005.C9 under TEN-000@1.
- **SPEC-003.C19a:** Public vars are defined and documented in their `millstrand.api.*.alpha` namespace, with public specs and `s/fdef` registrations retaining alpha-qualified names. A module may move plumbing to sibling `millstrand.api.<module>.internal[.<concern>]` namespaces, but it never re-exports public vars from them, and internal namespaces never require an alpha namespace. A validator or parser whose implementation is the grammar documents that grammar in its public docstring instead of maintaining a duplicate spec. `quality.api-form`, run by `quality.conventions-check`, enforces public-var docstrings, the 180-column limit, alpha/internal dependency direction, and the conversion ratchet. SPEC-005.P1/C5b own the public/internal boundary.
- **SPEC-003.C19b:** `millstrand.api.graph.alpha/readiness-where` is the canonical data-only where expression for readiness: `[:and [:= :state "active"] [:not [:edge/out "depends-on" [:= :state "active"]]]]`. Applying it through `millstrand.api.weaver.alpha/list` is equivalent to the built-in `ready` path; it does not change the strand-model query grammar or readiness semantics.
- **SPEC-003.C20:** `register-pattern!`, `replace-pattern!`, and `unregister-pattern!` are `millstrand.repl`'s runtime-implied, in-process counterparts to the explicit-runtime `millstrand.api.patterns.alpha` verbs. The registration and replacement arities accept a pattern name, optional non-blank doc string, fully qualified function symbol, and input spec name. Their owner-partition contract is SPEC-003.C23: a caller may replace its own entry, another owner's entry requires `replace-pattern!`, and retraction restores any shadowed entry.
- **SPEC-003.C23:** The direct per-entry registration functions across the graph, patterns, events, hooks, and weaver APIs (the explicit-owner `put-entry!`/`replace-entry!`/`remove-entry!` core seam and the domain APIs layered over it) remain trusted sharp tools, but every write carries an explicit owner: a direct owner may replace its own keys, while cross-owner replacement requires explicit override intent. Every kind exposes the same three verbs, spelled in that kind's own noun — ops `register-op!`/`replace-op!`/`unregister-op!`, queries `register-query!`/`replace-query!`/`unregister-query!`, patterns `register-pattern!`/`replace-pattern!`/`unregister-pattern!`, hooks `register-hook!`/`replace-hook!`/`unregister-hook!`, and event handlers `register-handler!`/`replace-handler!`/`unregister-handler!` — so the live loop is available for every kind rather than ops alone. `register-*!` claims a name in the caller's own partition and is loud when another owner already supplies it; ops are stricter still and reject any existing name, their own included. `replace-*!` is the deliberate override, loud when the name is absent, and it records the intent that carries a shadow across `refresh!`. `unregister-*!` retracts only the caller's own entry — it cannot delete another owner's, so retracting a shadow restores the shadowed entry rather than removing the name — and returns `{:unregistered <canonical key>}` for every kind. `replace-*!` returns exactly what its kind's `register-*!` returns. Module contribution publication reuses the same validators and effective-registry shapes without calling these functions entry by entry. Known constraint (**F20**): a direct write is not serialized against an in-flight `refresh!`. Publication resets each core registry to the candidate snapshot captured after evaluation and source loading, so a direct write landing in the narrow staging span between that capture and publication is silently overwritten; the window is small (only staged publication sits inside it), and reconcile-time direct writes run after publication and are safe. This trusted-REPL posture is accepted rather than lock-serialized (TEN-002); a caller needing a durable direct write across a concurrent refresh sequences it outside the refresh.
- **SPEC-003.C23a:** `millstrand.api.registry.alpha` is the blessed kind-declaration and owner-partition primitive for spool domains (SPEC-005.C2). It declares a registered kind — id, entry spec, binding-moment datum, and layer policy — so a declared kind becomes a valid key the refresh kernel publishes from a module contribution (SPEC-004.C46), and it exposes the direct owner-partition operations: replace or remove one complete owner partition, read immutable effective snapshots, and explain the active, shadowed, and override entries for a kind. It carries no generic resource or effect callbacks; domains do their baseline, durable-write, and lifecycle work through their own APIs around publication. The core owner-registry implementation it fronts stays internal (`millstrand.core.*`).
- **SPEC-003.C23b:** A kind declaration may name an optional `:candidate-validator`: a fully qualified symbol the refresh coordinator resolves through the generation classloader and calls once per refresh with `{:runtime :kind :entries :owners}`, where `:entries` is the kind's complete effective candidate entry map and `:owners` maps each entry key to its winning owner. It runs after every owner's contribution is staged and before any publication, so it is the seam for rules a per-entry `:entry-spec` cannot state: references between entries different owners contributed, deletions an owner expressed by omission, and resolvability of the symbols an entry names. Throwing rejects the whole refresh before `publish!`, so every affected owner retains its previous live partition (SPEC-004.C46, C96). It is a validator, not a reconciler: it performs no registration, no durable write, and no resource effect.
- **SPEC-003.C23c:** `millstrand.api.spec.alpha` is the blessed spec-over-wire documentation projection (SPEC-005.C2): JSON-safe, data-only views of a registered clojure.spec — `spec-forms` (the PROP-Wcd-001.S11 printed-form graph, entries enriched with predicate-var `doc`/`private` metadata), `contract` (a nested node tree interpreting `s/keys`, `coll-of`/`every`, `s/and`, `s/or`, `s/nilable`, `s/map-of`, `s/tuple`; an `s/and` uses its first interpreted structural form as the shape, falling back to its first form only when none exists, and retains every other form as a constraint), `template` (a copyable JSON skeleton with doc-bearing placeholders), `contract-template` (the same skeleton rendered from an already-built `contract` node tree, so a caller may enrich the tree — authored per-key docs, for example — and re-render it), `explain-text` (`s/explain-str` as plain text), `problems` (structured per-problem entries with structural missing-key detection), and `projection` (the composite bundle). Invariants: the projection is documentation, never a schema — the live registered spec stays the validation authority; no predicate is ever invoked (enrichment reads var metadata only, without loading code); any operator or form the walk does not recognize emits its printed form verbatim, so the view can summarize but cannot disagree with the live spec; re-entering a spec mid-expansion emits a `ref` node (the cycle cut). The exact node schema and placeholder grammar are owned by the module's docstrings (SPEC-003.C19a); wire surfaces embedding the projection reuse its field names rather than minting per-surface shapes.

- **SPEC-003.C23d:** `millstrand.api.errors.alpha` is the blessed error-authoring surface (SPEC-005.C2): factories that throw an `ex-info` shaped for the keys the CLI renders as affordances. `not-found!`, `invalid-argument!`, and `conflict!` each take `message` and a details map, with an optional trailing `cause`, and throw through `millstrand.api.spool.alpha/fail!` — the single throwing seam; `remedy` returns a details map with `:try` stamped. Each factory names the keys a good error of its kind carries and fails loudly when one is missing: `not-found!` requires `:token`, `invalid-argument!` requires `:token` plus `:expected` or `:available`, `conflict!` requires `:try`. Every rendered key the map carries is checked against its shape whichever factory threw — `:code`, `:canonical-query`, and `not-found!`'s `:token` are names (non-blank string, keyword, or symbol), `:available` is a non-empty collection of names, `:try` is a non-blank string — because a value the renderer cannot read is dropped in silence. `invalid-argument!`'s `:token` is deliberately unconstrained: a rejected argument is as often a number, a map, or `nil` as a name. Two properties are contract. No factory stamps a `:code`, so an absent code still reaches the weaver's own inference, including the `query/not-found` a failed canonical-query lookup owes its callers (SPEC-004.C36b); and every key outside the checked set passes through untouched into the error details, so the details map stays open. The factories are a convenience, never a gate: any op may throw a bare `ex-info` and render (SPEC-005.C7 keeps codes and message text non-contract, and the renderer switches on neither). The exact per-factory grammar is owned by the module's docstrings (SPEC-003.C19a); the author-facing convention is [Writing shared spools](../../docs/spools/writing-shared-spools.md).
- **SPEC-003.C24:** `(runtime/reload-code! runtime lib)` accepts a qualified library symbol present in the running generation's tools.deps basis. The input retains the public `:millstrand.api.runtime.alpha/root-lib` spec name with basis-lib semantics, and the returned closed map conforms to `:millstrand.api.runtime.alpha/reload-code-result`. The result contains exactly `:lib`, `:status`, and `:namespaces`; `:status` is `:reloaded` or `:unchanged`, and `:namespaces` is a vector of qualified namespace symbols in reload order. The function resolves source-backed classpath entries already recorded for that library, reloads their loaded namespaces in dependency order through the one generation classloader, and never resolves, adds, removes, or replaces a coordinate. An absent library, a library without a source-backed classpath entry, a malformed input, or a reload failure throws `ExceptionInfo` with the library and failing stage. The function and `s/fdef` consult the two named specs at call and return boundaries, and its docstring cites this clause. Publication and lifecycle reconciliation remain the job of `refresh!`.
## SPEC-003.P4 Runtime transformation helpers

Millstrand ships blessed source-visible runtime transformation namespaces for trusted config, activated spools, and live in-weaver REPL forms. Except for client-side `millstrand.api.peers.alpha`, these namespaces take an explicit runtime first argument and never route through connected-client state:

- `millstrand.api.graph.alpha` exposes the data-only `readiness-where` value, `(query-ids runtime query params)`, `(burn-by-ids! runtime ids)`, `(burn-by-ids! runtime ids req-ctx)`, `(strands-by-ids runtime ids)`, `(ancestor-root-ids runtime seed-ids)`, `(ancestor-root-ids runtime seed-ids opts)`, `(subgraph runtime root-ids)`, `(subgraph runtime root-ids opts)`, `(incoming-edges runtime to-ids edge-type)`, `(outgoing-edges runtime from-ids edge-type)`, `(register-query! runtime query-name query-def)`, `(replace-query! runtime query-name query-def)`, `(unregister-query! runtime query-name)`, `(queries runtime)`, `(resolve-query runtime query-name)`, and `(query-explain runtime query-name)`. `readiness-where` is the canonical readiness predicate for composing registered queries. Traversal opts may include `:type` for the declared acyclic relation to walk and default to `"parent-of"`; annotation relations fail loudly. `ancestor-root-ids` also preserves `:where`/`:params` filtering. `incoming-edges`/`outgoing-edges` are lenient single-hop adjacency lookups (an absent id yields no rows rather than a missing-id error). `register-query!` validates and merges a named query definition into the runtime registry; `queries`, `resolve-query`, and `query-explain` read it back. `burn-by-ids!`'s `req-ctx` arity threads an explicit request-context map (the same shape `millstrand.api.batch.alpha/apply!` accepts) into the `:strand/burn-before-commit` validation gate; the two-argument form derives its own burn context.
- `millstrand.api.patterns.alpha` exposes `(register-pattern! runtime name fn-sym input-spec)`, `(register-pattern! runtime name doc fn-sym input-spec)`, the matching `replace-pattern!` arities, `(unregister-pattern! runtime name)`, `(patterns runtime)`, `(resolve-pattern runtime name)`, `(explain runtime name)`, `(weave! runtime name input)`, and `(weave! runtime name input req-ctx)`. Pattern functions are weaver-loadable function symbols called with `{:input input}` after input spec validation. `resolve-pattern` matches `millstrand.api.graph.alpha/resolve-query` naming; the interactive surface does not expose pattern reads per SPEC-003.C21. The `req-ctx` arity threads an explicit request-context map (the same shape `millstrand.api.batch.alpha/apply!` accepts) for trusted callers such as the connected-client tier; the three-argument form derives its own weave context.
- `millstrand.api.events.alpha` exposes `(register-handler! runtime key types fn-sym)`, `(register-handler! runtime key types fn-sym metadata)`, the matching `replace-handler!` arities, `(unregister-handler! runtime key)`, `(handlers runtime)`, and `(recent-failures runtime)`. Registration validates the stable key, non-empty keyword type set, weaver-resolvable function symbol, and data-first metadata, replaces prior entries by key within the registering owner's partition, and returns the data-first entry. Event submission is not public surface: mutation operations submit events internally through the dispatch lane (SPEC-004.C73), and the blocking event-lane quiescence await ships as the test primitive `millstrand.test.alpha/await-quiescent!` (SPEC-004.C74b).
- `millstrand.api.hooks.alpha` exposes `(register-hook! runtime key types fn-sym)`, `(register-hook! runtime key types fn-sym opts)`, the matching `replace-hook!` arities, `(unregister-hook! runtime key)`, and `(hooks runtime)`. Hook keys are stable keywords, symbols, or non-blank strings; hook type sets are non-empty keyword sets; function symbols are fully qualified and weaver-resolvable; `opts` may include `:order` and data-first metadata. Registration replaces by key within the registering owner's partition, `hooks` returns deterministic data-first entries, validation hooks return normally or throw, and transform hooks return `{:hook/value replacement}`.
- `millstrand.api.peers.alpha` exposes `(peers)` (data-first sibling weaver metadata rows with staleness) and `(call! peerish op args)` (invoke one allowlisted public JSON socket operation; a name or explicitly path-like workspace resolves one running peer, and unknown/stale/ambiguous peers fail loudly). Both are client-side: they read published runtime metadata and speak to peer sockets from the calling process, behaving identically inside the live weaver JVM and in explicit connected client/test workflows. See the Weaver Runtime spec, SPEC-004.P10c.
- `millstrand.api.current.alpha` exposes `(runtime)`, the blessed facade for reading the active in-process weaver runtime. It fails loudly when no active runtime exists and never falls back to connected-client state. Its non-throwing sibling `(runtime-or-nil)` is the single sanctioned way to *probe* for an ambient runtime, returning nil rather than throwing so trusted resolvers can branch on absence instead of catching an exception as control flow. It also exposes `(with-runtime* runtime thunk)` and the `(with-runtime runtime & body)` macro, the blessed scoping twins that bind `runtime` as the thread-local ambient runtime for a dynamic extent so nested `(runtime)` reads and explicit-runtime callees agree without threading the runtime through every call.
- `millstrand.api.batch.alpha` exposes `(apply! runtime payload)` for transactional batch graph mutation payloads with `:refs`, `:strands`, `:edges`, and `:burn`. It returns normalized Clojure data from the weaver operation, including final refs, created rows, updated before/after rows, burned rows, and edge outcomes, without a JSON envelope.
- `millstrand.api.format.alpha` exposes the pure, runtime-free prose helpers `(prose template scope)`, `(fill block)`, and `(reflow block)`. `prose` baseline-dedents a Markdown template, preserves its remaining layout, and interpolates named scope values with `{name}` or compact JSON with `{name:json}`; invalid indentation, missing values, and unsupported renderers fail loudly. `fill` and `reflow` retain their `|`-margin contracts and fail loudly on a block with no barred lines (TEN-003) instead of returning empty output.

Helpers execute weaver-side when called from `init.clj`, activated runtime spools, or the live weaver REPL after the caller passes an explicit runtime. Connected client users who want to register new pattern, event handler, or hook functions should place them in weaver-loadable config/spool code and register their symbols through `millstrand.repl` or trusted `millstrand.core.client` calls. Pattern, event, and hook registrations are weaver-lifetime runtime state unless user config reloads them on startup.

User config may require `millstrand.api.graph.alpha`, `millstrand.api.patterns.alpha`, `millstrand.api.events.alpha`, `millstrand.api.hooks.alpha`, `millstrand.api.batch.alpha`, and `millstrand.api.format.alpha` so users can inspect and extend the blessed path. These built-in namespaces come from the Millstrand checkout on the Weaver classpath.

Hook functions receive one context map and run synchronously at the lifecycle gates specified by the Weaver Runtime contract. They may reject by throwing; only explicit transform hook families may replace values. Hook registration, unregistration, and introspection are trusted Clojure workflows and are not public JSON socket operations.

Event handlers receive one event map and may perform trusted side effects, including calling Millstrand APIs. They are dispatched asynchronously after successful mutation commits; handler return values are ignored. `(events/recent-failures)` returns bounded, data-first failure records for handler exceptions. Handler exceptions do not fail the already-committed mutation.

## SPEC-003.P4a Scheduler helpers

`millstrand.api.scheduler.alpha` is a blessed source-visible explicit-runtime namespace for the weaver-owned no-poller scheduler primitive (SPEC-004.P10d), usable from trusted config, activated spools, and live in-weaver REPL forms. Like the rest of the P4 helpers it takes the target runtime as its first argument for every operation, never performs connected-client routing or implicit ambient lookup, and expects callers to capture the runtime with `millstrand.api.current.alpha/runtime` only at trusted entry points.

- **SPEC-003.C58:** `(schedule! runtime wake)` persists or replaces one durable wake and arms it. `wake` is a map with a stable `:key`, an absolute `:wake-at` (`java.time.Instant`), a fully qualified `:handler` symbol resolvable in the weaver classloader, and an optional `:payload` (nil or a JSON-object-encodable map). Replacing an existing key resets its attempt count. Unknown keys, malformed values, unsupported payloads, and unresolvable/non-callable handlers fail loudly and persist nothing.
- **SPEC-003.C59a:** `(cancel! runtime key)` cancels a pending wake by stable key and returns data-first cancellation information; a missing key fails loudly. No separately named idempotent cancel helper ships.
- **SPEC-003.C59b:** `(pending runtime)` returns serializable data-first maps for pending scheduler wakes. It never exposes functions, executors, timer handles, or raw JDBC rows. The earliest pending wake is `(first (pending runtime))`, since `pending` is ordered by wake-at ascending; no dedicated public accessor is exposed.
- **SPEC-003.C59c:** Handler symbols resolve in the weaver JVM/runtime classloader, matching event/pattern conventions, and are persisted as a symbol/string representation that survives restart; closures and anonymous functions are rejected. Handlers receive one context map (SPEC-004.C103), run through the weaver's serialized async lane, have their return value ignored, and exceptions do not crash the scheduler.

## SPEC-003.P4b Native process custody

This section describes the shipped Mill-owned custody API.

`millstrand.api.process.alpha` is the trusted in-process surface for native work that must continue while Mill replaces its Weaver. It takes the active runtime explicitly, is backed by the Weaver-to-Mill custody channel, and is not a `strand` op or a public JSON socket operation. Mill owns process identity, process-tree cancellation, output retention, and completion facts; the calling spool owns the durable record and the domain transition applied during reconciliation.

- Boundary ownership is explicit: `millstrand.core.specs` owns `:millstrand.core.specs/process-launch-spec`, `:millstrand.core.specs/process-record`, `:millstrand.core.specs/terminal-process-result`, and `:millstrand.core.specs/process-acknowledgement-result`; the wire adapter owns `:millstrand.core.weaver.process-protocol/control-request` and `:millstrand.core.weaver.process-protocol/control-response`, plus `validate-request!` and `validate-response!`.
- **SPEC-003.C71:** `millstrand.api.process.alpha` exposes exactly `(launch! runtime owner key launch-spec)`, `(get runtime handle)`, `(list-owned runtime owner)`, `(cancel! runtime owner handle)`, and `(acknowledge! runtime owner handle)`. `owner` is a stable spool-owned keyword and `key` is a non-blank owner-scoped string derived from the caller's durable record; neither may be a PID. `launch-spec` is a closed map with a non-empty vector of strings `:argv`, absolute string `:cwd`, string-to-string `:env` additions, and optional string-or-nil `:stdin`. Mill passes `:argv` directly to the operating system; it does not parse a shell command or interpolate tokens. Mill validates the complete launch spec at this boundary and fails loudly on malformed values, missing paths, or unsupported process state; the API does not accept a shell command string or a function.
- **SPEC-003.C72 (launch idempotency):** `launch!` atomically reserves `[owner key]` and starts one process tree. A repeated call with the same owner, key, and launch spec returns the existing record in its current phase (`:starting`, `:running`, or `:terminal`). Reuse with a different launch spec fails loudly. The reservation remains after acknowledgement, so the same owner/key cannot launch a second process during the selected Mill lifetime. The returned process record contains an opaque non-blank `:handle`, `:owner`, `:key`, `:phase`, and `:output` references. A terminal record contains exactly one of `:exit {:code integer :signal nil-or-string}`, `:cancellation {:reason non-blank-string}`, or `:launch-failure {:message non-blank-string}`.
- **SPEC-003.C73 (process records):** `get` returns one process record by opaque handle. `list-owned` returns every unacknowledged record for the owner, including `:starting`, `:running`, and `:terminal` phases, and is the startup reconciliation surface. Output references identify Mill-retained stdout and stderr facts without exposing a PID contract. A terminal `:exit` records the process exit status, a terminal `:cancellation` records Mill's cancellation reason, and a terminal `:launch-failure` records exactly one non-blank failure message when Mill could not start the process. Unknown handles, owner mismatches, malformed records, and contradictory phase/result shapes fail loudly.
- **SPEC-003.C74 (cancellation and acknowledgement):** `cancel!` and `acknowledge!` require both the caller's `owner` and the opaque `handle`; Mill rejects an owner mismatch before acting. `cancel!` requests cancellation of the whole process tree and is idempotent; it does not erase the record, and the resulting terminal cancellation or exit remains visible through `get` and `list-owned`. `acknowledge!` accepts only a terminal handle owned by the caller, removes that record from `list-owned`, and permits Mill to clean retained output. Acknowledgement retains the owner/key tombstone and never permits relaunch of the acknowledged key during the selected Mill lifetime. A missing, non-terminal, or owner-mismatched handle fails loudly.
- **SPEC-003.C76 (custody boundary):** The process API transfers only its data-first launch specifications and process records. It does not transfer JVM callbacks, Java `Process` waiters, or in-memory closures, and it may return the same terminal record until `acknowledge!` succeeds. Continuity across a new Mill process is outside this API contract.

## SPEC-003.P5 Runtime workspace helpers

`millstrand.api.runtime.alpha` is the blessed alpha namespace for trusted configuration, active spools, and live in-weaver REPL workflows. Runtime helpers take an explicit runtime first. Trusted entry points capture the active runtime once with `millstrand.api.current.alpha/runtime` and thread it explicitly.

- **SPEC-003.C25:** `<selected-workspace>/init.clj` is the shared activation file. Optional `<selected-workspace>/init.local.clj` is read second and may replace a shared module declaration by declaring the same owner key. Both files contain ordinary trusted Clojure and call `(runtime/module! runtime owner opts)` explicitly; dependency presence never activates code. Startup and a full `refresh!` evaluate each present file in that order. An absent file contributes no declarations for its layer. An unreadable file, an evaluation error, or an invalid declaration fails at the activation-file boundary with the resolved path and original cause. Activation-file outcomes conform to `:millstrand.api.runtime.alpha/activation-file-result`, a closed map containing exactly `:path`, `:layer` (`:shared` or `:local`), and `:status` (`:loaded` or `:absent`). The runtime API namespace docstring and `refresh!`/`plan` docstrings cite this clause and that spec.

- **SPEC-003.C25a:** `(runtime/module! runtime owner opts)` declares one owner under a stable keyword. `opts` is closed by `:millstrand.api.runtime.alpha/module-opts` to exactly one of `:ns` or selected-workspace-relative `:file`, plus optional `:load :image`, `:after`, and `:required?`; callbacks and unknown keys are invalid. `:load :image` is valid only with `:ns`. The normalized declaration conforms to `:millstrand.api.runtime.alpha/module-declaration`, and the staged or refreshed return conforms to `:millstrand.api.runtime.alpha/module-result`. `module!` validates owner, options, normalized declaration, and return value through those specs, and its docstring cites them and this clause. File targets may not escape the selected workspace. Namespace targets resolve through the running generation classloader. Image mode uses only a current retained declaration record. Missing, stale, foreign, or malformed records fail as that module's evaluation outcome.

- **SPEC-003.C25b:** Each successful module source evaluation collects the typed `use-*!` selections, combined `def*!` selections, open-kind declarations, and lifecycle declarations for that owner. The collected set is complete: publication atomically replaces that owner's prior registry partitions and lifecycle declarations, so omission retracts prior entries. The collected value conforms to `:millstrand.api.runtime.alpha/module-collection`; the candidate module outcome conforms to `:millstrand.api.runtime.alpha/module-outcome`. Collection, candidate assembly, and publication consult those specs at their boundaries. The `collect-entry!`, `collect-kind!`, authoring-form, `module!`, `plan`, and `refresh!` docstrings cite this owner-complete contract. A failed source evaluation or invalid collection publishes nothing and leaves the owner's current image unchanged.

- **SPEC-003.C25c:** A full `(runtime/refresh! runtime)` re-reads both activation files and reconciles source, module graph, owner-complete contributions, and lifecycle resources already available to the running generation. Live changes are edits to a declared `:ns` or `:file` source, module additions or omissions, `:after` or `:required?` changes, owner-complete selection changes, and lifecycle declaration changes. Targeted refresh retains the existing `{:only [owner ...]}` grammar, includes affected dependants, does not re-read activation files, and cannot remove an unselected owner. Neither arity acquires a library or changes a classpath.

- **SPEC-003.C26:** `:millstrand.api.runtime.alpha/basis-fingerprint` is the public alias of `:millstrand.core.specs/basis-fingerprint` and accepts the `sha256:<64 lowercase hexadecimal digits>` encoding fixed by SPEC-004.C45. `:millstrand.api.runtime.alpha/basis-change` is the public alias of `:millstrand.core.specs/basis-change` and owns exactly `{:running-fingerprint F :candidate-fingerprint F2}`, where both values conform to `basis-fingerprint` and differ. `:millstrand.api.runtime.alpha/dependency-diagnostic` is the public alias of `:millstrand.core.specs/dependency-diagnostic` and owns exactly `{:status :invalid-dependency-config :stage S :source-path P :message M :cause C :coordinate X}`. `S` is `:deps-read` or `:deps-resolve`; `P`, `M`, and `C` are non-blank strings; and `X` is nil or exactly `{:lib qualified-symbol :value edn-value}`. Nil is the explicit outcome when tools.deps cannot isolate one coordinate. These are the only REPL-facing basis and dependency-failure field names. The public aliases, `refresh-result`, `plan-result`, and `status-result` are registered in `millstrand.api.runtime.alpha`, are cited by the `refresh!`, `plan`, and `status` docstrings, and are consulted when each candidate basis and public result is assembled.

- **SPEC-003.C26a:** Before evaluating either activation file or module source, full refresh composes and resolves a candidate basis only for comparison with the running fingerprint. A difference returns exactly `{:status :restart-required :reason :dependency-basis-changed :basis B}`, where `B` conforms to `basis-change`; no pending activation or source change becomes visible. Invalid dependency data returns the exact `dependency-diagnostic` map from C26 at top level. It does not wrap that map or replace its preserved cause. Current-basis refresh returns the `:applied`, `:partial`, or `:unchanged` module/lifecycle outcome data. The input conforms to `:millstrand.api.runtime.alpha/refresh-opts`, the union conforms to `:millstrand.api.runtime.alpha/refresh-result`, `refresh!` consults both at call/return boundaries, and its docstring cites C26–C26a. A missing canonical `deps.edn` is an invalid `:deps-read` result naming its resolved path and remediation; it never produces a default or empty basis.

- **SPEC-003.C26b:** `(runtime/plan runtime)` and its targeted arity perform the same reads, candidate-basis comparison, source evaluation, collection, and validation as `refresh!`, but do not publish, reconcile, mutate coordinator state, or replace retained image records. The result has the same dependency short-circuit variants as C26a, and current-basis plans contain activation-file, module, contribution, lifecycle, resource, namespace, and conflict intentions. Inputs conform to `:millstrand.api.runtime.alpha/refresh-opts`; output conforms to `:millstrand.api.runtime.alpha/plan-result`. `plan` consults both specs and names them with this clause in its docstring.

- **SPEC-003.C26c:** `(runtime/status runtime)` is an offline read and returns a closed `:millstrand.api.runtime.alpha/status-result` containing exactly `:basis-fingerprint`, `:modules`, `:resources`, `:loaded-namespaces`, and `:last-refresh`. Its fingerprint conforms to the public spec in C26; status does not re-read dependency files and exposes no candidate fingerprint, family, root, sync, acquisition, or pending-generation state. `(runtime/resolve-var runtime sym)` requires a qualified symbol conforming to the retained `:millstrand.api.runtime.alpha/resolvable-symbol` spec, resolves and, when needed, requires its namespace through the one generation classloader, and returns a Var or nil when that namespace defines no Var at the name. Resolution and load failures propagate with symbol and namespace context; it never searches another loader or resolves dependencies. `status` and `resolve-var` consult their named input/return specs through `s/fdef`, and both docstrings cite this clause.

- **SPEC-003.C27:** A restart-required or invalid-dependency result changes no live module, registry, lifecycle, resource, retained-image, or classloader state. Taking the edited basis into use requires the replacement flow in `SPEC-004.C113`–`C123`; this API neither starts replacement nor retries an interrupted call.

## SPEC-003.P5b Blessed op argv parser

- **SPEC-003.C60:** `millstrand.api.cli.alpha` is the blessed declarative argv parser for weaver ops: a data-first arg-spec (named typed flags, including value-consuming `:string`, `:int`, `:boolean-token`, presence `:boolean`, `key=value` `:map`, repeatable flags, positionals, per-arg docs, payload-parse declarations) parses envelope argv into a data map or throws a loud structured error naming the offending token and the op's spec. The same arg-spec powers `help <op>` rendering via a JSON-safe `explain` projection. `millstrand.api.return-shape.alpha` is the blessed pure return-shape companion shared by registry validation, help, author tests, and real spool-to-spool consumption seams; it exposes `validate!`, `explain`, and `check!` without runtime, registry, socket, or CLI parser state. Spool authors may layer clojure.spec/malli on the parsed input map; both declaration surfaces stay data-first.
- **SPEC-003.C60a:** A return shape is a finite inline EDN tree. Scalar leaves are `:string`, `:integer`, `:number`, `:boolean`, `:null`, and `:json`; `:json` accepts any JSON-compatible value. `[:nullable <scalar>]` accepts nil or `:string`, `:integer`, `:number`, or `:boolean`; it cannot wrap maps, collections, `:json`, `:null`, or another nullable form. Maps declare `{:type :map :required {<keyword> <shape> ...} :optional {<keyword> <shape> ...} :extra <shape>}`. Required and optional default to empty, may not overlap, and undeclared keys fail checking unless `:extra` supplies their common value shape. Homogeneous sequential collections declare `{:type :collection :items <shape>}`. The language has no functions, arbitrary predicates, coercions, defaults, general unions, named references, or recursive declarations.
- **SPEC-003.C60b:** A flat non-stream op uses a shape directly. A subcommand op mirrors its arg-spec tree: an interior return node is `{:subcommands {<name-string> <return-node> ...}}`, and a leaf return node is a return case; names match the arg-spec exactly at every level. A streaming return case declares `{:stream {:emits <shape> :result <shape>}}`; a non-stream case is a shape. `validate!` returns valid authored data unchanged or fails loudly, `explain` returns the declaration as JSON-safe data, and `check!` returns a conforming value unchanged or throws structured mismatch data carrying at least the failing path, expected shape, actual value. Selection context carries the full subcommand path.
- **SPEC-003.C61:** Payload reference resolution is a parser contract: a **whole** argv value of `:stdin` or `:payload/<name>` resolves to the named envelope payload string; no substring interpolation. A reference without a matching payload fails loudly; an attached payload nothing references fails loudly. An arg may declare `:parse :json`/`:jsonl` to parse the resolved payload, failing loudly with context on malformed input. Every registered op declares an arg-spec, so argv and payload handling use the blessed parser.
- **SPEC-003.C62:** Trusted config and REPL code registers, replaces, and removes ops through `millstrand.api.weaver.alpha/register-op!`, `replace-op!`, and `unregister-op!`. Registry behavior, provenance, `:returns` metadata, workspace `defop`, and publication are owned by `SPEC-004.C63a`–`C63d`.

- **SPEC-003.C64:** An arg-spec node declaring `:subcommands` maps subcommand names to nested nodes of the same shape at any depth. A leaf declares no `:subcommands`; its `:flags` and `:positionals` are optional, so doc-only leaves are valid. An interior node may not also declare `:flags`, `:positionals`, `:hook-class`, or `:deadline-class`; an empty `:subcommands {}` is invalid. Every leaf of an op declaring an arg-spec carries `:hook-class` (`:read` or `:mutating`) and `:deadline-class` (`:standard` or `:unbounded`); a flat arg-spec root is its leaf. A streaming leaf declares `:deadline-class :unbounded`. One shared structural validator enforces these rules recursively. It rejects `help`, `-h`, `--help`, and the `subcommand` arg name at every level; `parse` and `explain` consult it, and the op registry reuses it for registration-time failure (SPEC-004.C63d).
- **SPEC-003.C65:** Parsing a subcommand arg-spec routes recursively: each argv token selects a child node until a leaf is reached, then the remaining argv parses against that leaf. The parsed map merges the leaf result with `:subcommand`, always the full path as a vector of name strings. A missing or unknown token fails loudly with canonical context: `:op` (string), `:path` (tokens already walked), `:token` (or nil when missing), and `:available` (child names at the failing node). Payload references and `:parse` declarations (SPEC-003.C61) apply unchanged at every depth. Structural validation, parsing, socket pre-hook walking, help slicing, and return selection use the same context shape.
- **SPEC-003.C66 (help projection):** `explain` projects the selected arg-spec node and matching return shape as JSON-safe data at every declared depth. `SPEC-002.C39` and `SPEC-002.C44` own the help envelope and uniform node shape; `SPEC-004.C107` owns resolved source. The projection supplies leaf `hook-class` and `deadline-class`, recursive children, argument declarations, return declarations, and C67 annotations for those owning contracts.
- **SPEC-003.C67 (per-node annotations — closed sub-map on the arg-spec node):** `use-when`, `notes`, and `failure-modes` are authored as a closed, validated annotation sub-map on each arg-spec node. `use-when` and `notes` are string arrays; `failure-modes` is an array of glossary outcome names. The structural validator validates closed keys and non-blank strings without runtime dependency. At registration, `register-op!` and `replace-op!` check every glossary reference against the live glossary; module publication does so after each generation's glossary contributions merge and before the generation becomes effective. The projection folds each node's annotations into that node; `about` prose remains cross-verb narrative.
- **SPEC-003.C68 (help paths vs parse depth):** Help may select any interior or leaf path declared by the arg-spec. Unknown tokens fail with the C65 context. `SPEC-002.C44` owns the rendered help node and path envelope.
- **SPEC-003.C69 (`about`/`prime` are arity-1):** `about` and `prime` accept one op name; only `help` accepts an arg-spec path. `SPEC-002.C46` owns meta-verb routing and failures, and `SPEC-004.C109` owns the op-declared prose.
- **SPEC-003.C70 (declared arg `:spec` — spec-over-wire at the CLI arg boundary):** A flag-spec or positional-spec may declare `:spec`, a qualified keyword or symbol naming a registered clojure.spec of the parsed value. The structural validator (C64) requires a qualified ident and nothing more, and the parser never consults the declared spec: it is documentation, and the consuming op stays the validation authority (C23c). `register-op!`/`replace-op!` and publication entry validation fail loudly when a declared name does not resolve in the process-global spec registry, mirroring the C67 glossary-ref discipline. The help projection (C66) embeds the blessed projection fields on that flag/positional entry — `spec` (the printed name, `null` when undeclared) plus `spec-forms`, `contract`, and `template` (C23c) — resolved live at envelope build; an unresolvable name fails the projection loudly (`discovery/arg-spec-unresolved`), never a silent drop. The reference text transform renders a `{spec <name>}` marker on the arg line with the copyable `template` block beneath it, and renders presence `:boolean` flags with no value placeholder (they accept no value form on the command line).

## SPEC-003.P6 Runtime activation example

A workspace makes a spool available in `deps.edn`:

```clojure
{:deps {my/module {:local/root "spools/my-module"}}}
```

It activates that library explicitly in `init.clj`:

```clojure
(require '[millstrand.api.current.alpha :as current]
         '[millstrand.api.runtime.alpha :as runtime])

(let [rt (current/runtime)]
  (runtime/module! rt :my/module {:ns 'my.module}))
```

A dependency does not activate code. Editing either dependency file requires a replacement generation; editing a workspace-relative `:file` module remains live through `runtime/refresh!`.

## SPEC-003.P7 Surface boundary

This API exposes the namespaces and functions defined by its retained clauses. Package installation, source fetching, plugin discovery, and CLI package authoring belong outside the REPL API.

## SPEC-003.P8 Author-side test helpers

- **SPEC-003.C28:** `millstrand.test.alpha` is the author-side test namespace. Its namespace and Var docstrings define the public helper surface; this clause does not maintain a second Var inventory. `activate-module!` accepts only `:after` and `:load :image`, and consults `:millstrand.test.alpha/bare-runtime`, `module-key`, `namespace-symbol`, `module-options`, and `module-refresh-outcome`. `collect-module-forms` consults its named specs and returns an owner-complete unpublished collection without dependency resolution, source loading, publication, or reconciliation.
- **SPEC-003.C28a:** `(millstrand.test.alpha/manual-clock instant)` returns an uninstalled Clock whose sleep advances virtual time and runs no pumps. `(set-clock! runtime clock)` installs a Clock and rejects non-Clock values. One manual Clock may be installed in only one runtime; once installed, its zero or positive sleeps run that runtime's clock pumps synchronously. `(advance! runtime duration)` requires an installed manual Clock and a strictly positive Duration, advances it, runs the same pumps, and returns the new Instant. Installing and advancing manual time are author-side test controls; production callers use `millstrand.api.runtime.alpha/clock` or `now` (SPEC-003.C17, SPEC-004.C1a).
- **SPEC-003.C29:** `with-weaver-world` creates or accepts an isolated workspace, writes requested `config.json`, mandatory `deps.edn`, optional `deps.local.edn`, `init.clj`, optional `init.local.clj`, and workspace-relative files, then starts the unpublished runtime. Its option boundary conforms to `:millstrand.test.alpha/weaver-world-options`, and its generated-file projection conforms to `:millstrand.test.alpha/weaver-world-files`; both specs are closed, are consulted before files are written and before the context is bound, and are named by the macro and fixture docstrings. Missing canonical `deps.edn`, malformed dependency data, startup failure, and cleanup failure retain the existing fail-loud and suppressed-cleanup behavior.
- **SPEC-003.C30:** `repl!` accepts a world context and a quoted form or Clojure source string, evaluates it through that world's nREPL transport with the runtime ambiently bound, and returns its EDN-readable value; evaluation, transport, or unreadable-result failures are loud. `check-op-return!` returns a conforming value unchanged and fails loudly when the registered op has no matching declaration or the value violates its selected flat, subcommand, or stream-channel return shape. `await-quiescent!` returns the runtime once its event queue is empty and no handler dispatch is in flight; its optional `:timeout-ms` is a positive integer, defaults to 10,000, and fails loudly on timeout.
- **SPEC-003.C31:** The world context conforms to the closed `:millstrand.test.alpha/weaver-world-context` spec and contains exactly `:config-dir`, `:state-dir`, `:data-dir`, optional `:db-path`, `:storage`, `:source`, `:runtime`, `:metadata`, `:timeout-ms`, and `:basis-fingerprint`. `:basis-fingerprint` conforms to the runtime API spec of the same name and identifies the basis used to launch the in-process generation. `with-weaver-world`, `weaver-world-fixture`, and their docstrings name and consult this context spec.
- **SPEC-003.C32:** `millstrand.test.alpha/spool-checkout-root` remains the author-side helper for finding the directory checkout that backs a spool source resource on the test classpath. It returns the nearest checkout root whose `deps.edn :paths` contains the supplying classpath entry, for use as an ordinary tools.deps `:local/root`; it does not approve, activate, or write dependency data. Its input conforms to `:millstrand.test.alpha/classpath-resource`, its return conforms to `:millstrand.test.alpha/local-root`, and both arities consult those specs. The function docstring names the specs and this clause. Missing, jar-backed, and structurally unmatched resources fail loudly as before.
